const character = 'thaen';

console.log(character);

const inputs = document.querySelectorAll('input');

console.log(inputs);

inputs.forEach(input => {
    console.log(input);
})

// use 'tsc sandbox.ts' to complie it to js

// 'tsc sandbox.ts -w' to watch change in file